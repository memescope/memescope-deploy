# Local-only secret for `wrangler dev` (this file is a template — copy to .dev.vars and paste your real key).
# Get a key at console.anthropic.com. .dev.vars is gitignored so it is never committed.
ANTHROPIC_API_KEY=sk-ant-...
