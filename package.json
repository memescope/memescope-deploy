{
  "name": "memescope-deploy",
  "version": "2.5.128",
  "private": true,
  "description": "MemeScope — The Meme Coin Scope & Scanner",
  "scripts": {
    "build": "bash build.sh",
    "deploy": "bash build.sh && wrangler deploy && bash restore-symlinks.sh",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
